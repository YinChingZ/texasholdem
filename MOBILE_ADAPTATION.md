# 手机与平板布局

## 牌桌

- 700px 及以下使用纵向座位模板；本人固定底部，八人桌由顶部一人、左右各三人与本人组成。
- 更宽视口使用横向椭圆桌面；横屏高度 500px 及以下启用紧凑布局。
- `tableLayout.js` 统一提供座位、桌面下注、公共牌与底池锚点。`ChipFlight` 使用同一套坐标，下注从桌面下注区域飞向底池，派彩从底池回到赢家座位。
- `PlayerSeat` 在各端共用。本人手牌直接接收私牌通道数据；对手只有已获准公开的牌才能显示。
- 360×640 起，默认八人对局无需滚动即可观察全桌。320px 与极短视口允许纵向滚动，保持字号和操作完整性。

## 操作与弹层

- 行动区有固定高度，加注面板向上覆盖牌桌，不挤压或移动座位。
- 手机加注提供数字输入、快捷金额与滑杆；输入无效时无法提交。回合变化立即关闭面板。
- 聊天／设置抽屉使用 Portal，避免被牌桌和行动区的层叠上下文遮挡。支持 Escape、焦点限制和返回触发按钮。
- 手机聊天为底部抽屉，横屏为侧抽屉；输入区适配可用视口高度和底部安全区。
- 大厅采用单列名单与底部开始区域。主题、退出与关闭房间位于菜单。

详见 `UI_REFACTOR_ACCEPTANCE.md` 中的预览和测试命令。
