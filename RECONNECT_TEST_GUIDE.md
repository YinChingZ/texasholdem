# 重连与自动续局验收

实现和协议见 [状态与会话协议 v2](docs/STATE_AND_SESSION.md)。服务端内存状态不跨重启保留。

## 自动验证

```sh
npm --prefix server test
npm --prefix client test
cd client
E2E_PORT=5178 E2E_API_PORT=3118 npx playwright test e2e/session-recovery.spec.js e2e/multiplayer-flow.spec.js --workers=1
```

## 实机验收

1. 两个浏览器分别加入房间，在大厅刷新。身份和房间保持不变，房主仍可开始。
2. 下注时刷新或切后台断网再回来。恢复原底牌、筹码及弃牌/全押状态，行动期限不会延长；若已超时则显示服务端当前状态。
3. 断线超过 30 秒再回来。座位与本场筹码仍然保留，点击“回到牌桌”从下一手参与。
4. 同一浏览器打开第二个标签页。旧页正常回应时，新页显示身份占用；确认接管后旧页显示已被接管，不自动抢回。
5. 连续断开房主和第二名玩家。30 秒后应由仍在线的玩家接任；旧房主回来不会抢回权限。
6. 全押发公共牌期间刷新。正确恢复阶段和自己的牌，原手牌只结算一次。
7. 结算后关闭弹窗。倒计时仍在，8 秒后自动续局；随时可查看上一手。此时刷新也能恢复倒计时和结果。
8. 请求暂停或结束。当前手继续正常结算，之后暂停或展示排行榜；暂停后可恢复。
9. 两人对局中一人退出。另一人进入可恢复的等待状态，可以补人或结束后重新准备，不必关闭房间。
10. 旁观者刷新、退出、申请入座。角色正确恢复；手牌进行中申请排队，手间有空位时加入。
11. 房主手间释放离线座位。原身份回来重新入座保留原筹码；输光身份本场不能补筹码。
12. 关闭服务端再刷新或重连。明确显示房间失效并允许返回首页，不停在无限加载中。

全部成员离线后不再开始新手牌，连续无人在线 30 分钟回收房间。主动返回首页不清除其他标签页的共享凭证。没有账号体系，因此新建全新身份仍可绕过同一身份的筹码限制。
