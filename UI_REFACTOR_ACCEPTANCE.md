# UI 重构阶段验收

## 阶段 0 + 阶段 1

本阶段完成工程验收设施、现代牌室设计令牌、白天/夜间双主题、基础 UI 组件、欢迎页以及连接状态页。大厅和游戏桌仍使用旧版界面，等待下一阶段替换。

### 本地启动

```bash
cd client
npm install
npm run dev
```

### 确定性预览

开发环境支持 `?uiPreview=<state>`，可用状态：

- `welcome`
- `connecting`
- `reconnecting`
- `disconnected`
- `lobby-host`
- `lobby-guest`
- `game-turn`
- `game-waiting`
- `spectator`
- `result`
- `leaderboard`

例如：`http://localhost:5173/?uiPreview=welcome`。可追加 `&theme=light` 或 `&theme=dark` 固定预览主题。

### 自动化验收

```bash
npm run build
npm run lint
npm run test:unit
npm run test:e2e
npm run test:visual
```

视觉基线包含白天模式欢迎、连接中和断线页面的 1440×900、768×1024、390×844 截图，以及夜间欢迎页的桌面和手机截图。

### 人工检查

- 欢迎页在桌面为左右分栏，在平板和手机为单列。
- 首次访问默认白天模式；主题切换刷新后仍保留选择。
- 未填写昵称时创建按钮不可用；昵称和房间号齐全后加入按钮可用。
- 使用 Tab 可依次访问昵称、创建、房间号和加入按钮。
- 手机宽度没有横向滚动，输入框字号不小于 16px。
- 连接、重连、断线页面文案和房间号显示正确。
- 断线页面的“重新连接”按钮可调用原有重连逻辑。

## 阶段 2：等待大厅

本阶段已将等待大厅替换为多端共享的现代牌室界面，保留原有 Socket.IO 事件与房间规则。大厅覆盖房主、普通玩家和旁观者权限；桌面使用右侧聊天栏，900px 及以下使用带未读计数的 Bottom Sheet。

### 确定性预览

- `lobby-host`：四人房主视图与牌局设置
- `lobby-guest`：普通玩家视图
- `lobby-one`：单人不可开始状态
- `lobby-full`：八人满员状态

以上状态均可追加 `&theme=light` 或 `&theme=dark`。例如：`http://localhost:5173/?uiPreview=lobby-host&theme=light`。

### 自动化覆盖

- 单元测试：大厅权限、设置、开始条件、确认弹窗、聊天状态与 ViewModel。
- 功能 E2E：两个隔离浏览器上下文完成创建房间、输入房间号加入并开始牌局。
- 移动交互：390×844 下打开聊天 Sheet，并可用 Escape 关闭。
- 视觉基线：1440×900 房主、八人满员与夜间主题，768×1024 普通玩家，390×844 单人状态与聊天 Sheet。

### 人工检查

- 房主在 1 人时不可开始，达到 2 人后主按钮可用；访客始终显示“等待房主开始”。
- 房间号使用等宽字体；复制按钮通过 Toast 返回结果。
- 房主可更新“结算时显示所有手牌”和 500–50,000 范围内的初始筹码。
- 普通玩家可切换为旁观者；退出、旁观、关闭房间均使用统一确认弹窗。
- 桌面聊天常驻右栏；平板和手机通过底部按钮打开聊天，遮罩、关闭按钮和 Escape 均可关闭。
- 1440×900、768×1024、390×844 下无页面级横向滚动，主要触控目标不小于 44px。

## 阶段 3：桌面游戏桌与操作区

本阶段已将旧游戏页替换为单一墨绿牌桌舞台。本人固定在底部中央；旁观者视角以房主为底部锚点；其余玩家按照服务端数组顺序顺时针排列。桌面右侧保留 320px 聊天与游戏控制区，底部 HUD 合并本人手牌、筹码、下注状态和核心操作。

### 确定性预览

- 人数：`game-two`、`game-turn`（4 人）、`game-six`、`game-eight`
- 牌局阶段：`game-preflop`、`game-turn`、`game-turn-phase`、`game-river`、`game-showdown`
- 行动能力：`game-waiting`、`game-no-raise`、`game-all-in`
- 身份与结算：`spectator`、`result`、`leaderboard`

例如：`http://localhost:5173/?uiPreview=game-eight&theme=light`。

### 自动化覆盖

- 座位算法：2、4、6、8 人坐标唯一；本人锚点和顺时针服务端顺序保持稳定。
- 行动能力：过牌、跟注、不可加注、全押跟注、非本人回合与快捷加注金额。
- 真实功能 E2E：两个隔离浏览器完成创建、加入、开局，并由当前行动玩家完成一次合法过牌或跟注。
- 视觉基线：1280×720、1440×900、1600×900，覆盖 2/4/6/8 人、等待行动、不可加注、旁观、夜间主题和展开加注面板。

### 人工检查

- 当前玩家使用稳定暖金描边；弃牌、离线或筹码耗尽状态降低视觉权重，文字仍可读。
- 公共牌始终位于牌桌中央，总奖池位于公共牌下方；边池数量通过文字补充表达。
- 本人 HUD 首层只显示弃牌、过牌/跟注、加注；加注展开后提供快捷金额、滑块和确认按钮。
- 1280×720 首屏同时可见公共牌、本人手牌与主要操作，页面无横向溢出。
- 桌面右侧栏包含聊天、音效、退出、结束牌局和关闭房间；破坏性操作使用统一确认弹窗。
- 日间主题使用中等明度牌桌绿与暖白外围；夜间主题使用深石墨外围，均无蓝紫渐变、循环发光或整体缩放动画。

## 阶段 4：平板、手机竖屏与横屏

本阶段将游戏桌扩展为平板与手机共享布局。1024px 及以下不再渲染常驻侧栏，聊天和控制由顶部 44px 工具按钮打开；平板使用右侧抽屉，手机使用 Bottom Sheet。聊天和加注状态均保留在同一游戏页组件内，横竖屏切换不会重置。

### 固定视口

- `360×800`：手机竖屏最小验收宽度
- `390×844`：手机竖屏与八人玩家条
- `844×390`：低高度手机横屏
- `768×1024`：平板竖屏
- `1024×768`：平板横屏与夜间主题

以上视口使用 `game-turn`、`game-six` 或 `game-eight` fixture，可追加 `theme=light|dark`。

### 自动化覆盖

- Sheet：Escape、遮罩、关闭按钮、焦点循环入口和关闭后焦点回收。
- 聊天：Sheet 关闭时累积未读，打开后清零；仍使用 `sendMessage({ roomId, message })`。
- 软键盘：将 390×844 视口缩短到 390×500 后，聊天输入框与发送按钮仍在可视区域。
- 方向切换：打开加注面板后依次切换 390×844、844×390、390×844，滑块状态持续存在。
- 溢出：五个固定视口及横屏展开加注状态均检查 `scrollWidth <= innerWidth`。
- 视觉基线：五个主视口、手机聊天 Sheet、平板控制抽屉和横屏加注面板。

### 人工检查

- 手机顶部为紧凑房间状态栏、聊天按钮、控制按钮与主题开关，所有操作目标至少 44×44px。
- 手机玩家列表在牌桌顶部横向滚动，当前行动玩家使用暖金描边；列表滚动不会推动页面横向溢出。
- 手机底部 HUD 吸附在安全区内，公共牌、本人手牌和三个核心操作在首屏可见。
- 844×390 下左侧仅显示牌桌，右侧显示手牌与操作；聊天默认关闭。
- 横屏展开加注后，快捷金额、滑块和确认操作在右侧 HUD 内部滚动，不覆盖公共牌。
- 平板聊天和控制为右侧抽屉；手机为 Bottom Sheet；两者均支持键盘、遮罩和焦点回收。
- Sheet 使用 `100dvh` 与 `env(safe-area-inset-*)`，聊天输入保持 16px，避免 iOS 自动缩放。

## 阶段 5：结算、排行榜、音效与动效

本阶段完成牌局收尾界面的统一。手牌结算、最终排行榜和音效设置共用同一套哑光弹层、焦点管理与安全区规则；全局行动消息改为克制的顶部状态提示。所有现有 Socket.IO 事件、结算数据和房间权限保持不变。

### 确定性预览

- `result`：单一胜者、完整公共牌与摊牌明细
- `result-hidden`：仅公开胜者手牌
- `result-split`：两位玩家平分奖池
- `result-side-pot`：主池与边池由不同玩家获得
- `result-last-player`：只剩一位有筹码玩家，不再显示“开始下一手”
- `leaderboard`：最终排行榜与房主操作
- `message-allin`：全押全局消息

音效设置可在任一 `game-*` 预览中通过“音效设置”打开。以上状态均支持 `theme=light|dark`。

### 自动化覆盖

- 单元测试：卡牌数据兼容、排行榜排序、下一手事件协议、最后一名玩家操作、弹层 Escape/遮罩/焦点回收。
- 功能 E2E：结算弹层关闭、音效开关持久化、遮罩关闭和 `prefers-reduced-motion` 无循环动画。
- 视觉基线：1440×900 单胜者、768×1024 分池、390×844 边池、844×390 隐藏手牌、1440×900 排行榜、390×844 音效设置与 1280×720 全押消息。
- 阶段 0–5 全量结果：build、lint、38 个单元测试、12 个功能 E2E、40 个视觉 E2E 全部通过。

### 人工检查

- 结算弹层可滚动查看公共牌、获胜者、底牌和最佳五张组合；手机底部操作始终位于安全区内。
- 多位获胜者各自显示所得筹码；隐藏手牌时使用文字与锁图标共同说明，不只依赖颜色。
- 房主可返回牌桌、结束游戏或开始下一手；只剩一位有筹码玩家时不提供下一手；访客只能返回并等待房主。
- 排行榜使用对齐表格，冠军只使用暖金边线和文字强调，不使用金色渐变或庆祝粒子。
- 音效开关和音量写入原有 localStorage 键；试听按钮仍调用原有音效管理器。
- Escape、遮罩和关闭按钮行为一致，关闭后焦点返回触发元素；减少动画模式下无循环发光、翻转、位移或脉冲。

## 阶段 6：旧样式清理与最终回归

本阶段完成旧前端展示层清理并建立最终验收包。`mobile.css`、旧卡牌/数字/筹码动画组件、旧登录与房间占位组件均已删除；`index.css` 和 `App.css` 只保留全局基础设施，不再包含页面级响应式覆盖。

### 清理结果

- 删除 `mobile.css` 入口及约 1,000 行 `!important` 设备覆盖。
- 删除不再引用的 `AnimatedCard`、`AnimatedNumber`、`Card`、`ChipChangeIndicator`、`Login`、`Room` 和旧 `Welcome.css`。
- 删除所有 `[style*="..."]` 选择器、全局旧按钮渐变、蓝紫配色和遗留页面类名覆盖。
- `!important` 仅存在于 `prefers-reduced-motion` 的可访问性兜底规则。
- 唯一保留的 JSX `style` 是 `PlayerSeat` 的 `--seat-x/--seat-y` 动态坐标，用于 2–8 人确定性座位算法。
- 生产 CSS 从阶段 5 的约 76.7 KB 降至约 61.1 KB。

### 最终自动化覆盖

- 320、360、390、480、768、1024、1280、1600px 八档均验证无页面级横向溢出。
- 320×800 验证所有可见按钮至少 44×44px，并以文字表达“等待、思考中、已弃牌”等状态。
- 1600×900 在 200% 浏览器缩放下使用 800×450 有效 CSS 视口验收，公共牌、本人手牌和主要行动仍可操作。
- 所有开发预览链接逐一渲染并监听页面异常。
- 欢迎页键盘路径验证焦点顺序和可见焦点指示。
- 白天/夜间正文、次级文字、金色、成功、危险以及按钮前景/背景组合均自动验证不低于 4.5:1。
- 最终结果：build、lint、38 个单元测试、25 个功能 E2E、42 个视觉 E2E 全部通过。

### 最终视觉基线

阶段 0–6 的 42 张截图统一使用实体主题背景与最终设计令牌。新增：

- `320×800` 最小宽度完整游戏桌。
- `800×450`，对应 1600×900 在 200% 浏览器缩放时的有效视口。

所有历史阶段基线也已按最终全局字体、背景和通过对比度验证的语义颜色重新生成。

### 文档

- `MOBILE_ADAPTATION.md`：当前多端断点、组件职责、设计令牌和验收视口。
- `client/README.md`：前端结构、运行方式和五类验收命令。
- 本文档：阶段 0–6 的预览状态、自动化覆盖与人工验收清单。
