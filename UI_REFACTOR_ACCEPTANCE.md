# 扑克界面重构验收

## 设计与行为

- 首次访问默认深色，保留已有浅色选择；入口居中显示昵称、创建和加入房间。
- 独立大厅突出房间号、复制邀请、玩家名单与开始游戏。筹码和亮牌设置通过房主设置抽屉修改，退出与关闭房间在菜单内。
- 聊天默认收起，通过未读数提醒；桌面侧抽屉、手机底部抽屉，开启时不改变牌桌尺寸。聊天期间轮到本人时可以直接返回牌桌。
- 本人手牌与筹码合并在底部座位，行动区固定；加注浮层保留快捷金额、滑杆和数字输入。金额是服务端现有“额外加注”增量，不是加注后总额。
- 2–8 位玩家按座位布局展示。手机不使用横向玩家条，八人同时可见；筹码和桌面下注分别显示，数字不缩写。
- 总底池为主池加边池，不再累计桌面下注；边池可查看明细。
- 获胜者、赢得筹码和牌型优先显示，摊牌明细折叠。排行榜保留名次和筹码。
- 不修改服务端游戏规则、事件名称、权限或私牌分发。

## 本地检查

前端默认 `http://localhost:5173`，后端默认 `http://localhost:3000`。

```sh
cd client
npm run lint
npm test
npm run build
E2E_PORT=5174 npm run test:e2e
E2E_PORT=5174 npm run test:visual
```

`E2E_PORT` 默认 5173，`E2E_API_PORT` 默认 3100。建议使用 5174 避开正在操作的开发页面。测试前端通过 Vite 的 `/socket.io` 代理访问测试后端，避免新增测试端口触发跨域限制；不改变生产配置。代理目标由 `DEV_API_URL` 指定，默认本机 3000。

## 可重复预览

开发模式访问 `/?uiPreview=__index__` 查看所有状态，可追加 `&theme=dark` 或 `&theme=light`。

重点预览：`welcome`、`lobby-one`、`lobby-host`、`lobby-full`、`game-two`、`game-eight`、`game-stress`（长昵称与六位筹码／下注）、`game-reveal-eight`（合成八人公开手牌）、`game-side-pots`、`game-demo`、`result-split`、`result-hidden`、`leaderboard`。

## 验收覆盖

- 单元测试：房主与访客权限、筹码设置校验、额外加注协议、实时回合切换、全押跟注、结算动作、主题持久化、聊天与动画编排。
- 浏览器行为：真实双人加注到结算，以及八个独立浏览器上下文建房、加入、开局、私牌隔离、手机聊天、未读通知和合法行动。
- 布局压力检查：360×640、390×844、430×932、768×1024、1024×768、1280×720、1440×900、1920×1080、844×390，检查八人金额、下注、公共牌和本人手牌的可见性、裁切与交叠。
- 摊牌检查：手机、平板、桌面、横屏的公开手牌不遮挡筹码或彼此覆盖。
- 边界检查：320px 无横向溢出、200% 缩放等效视口可操作、软键盘压缩高度后的聊天输入、44px 触控目标、焦点恢复、深浅主题颜色对比和减少动态效果。
- 视觉基准保存在 `client/e2e/*-snapshots/`。更新前必须检查变化；不使用更新截图来掩盖布局或交互失败。

浏览器自动化当前使用 Chromium；Safari、Firefox 与实际手机上的表现仍需要对应设备回归。
